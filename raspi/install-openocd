cd `dirname $0`
. ./box-settings


./box-up-wait

./ssh " sudo apt update "
#./ssh " sudo apt -y install openocd "

./ssh " git clone https://git.code.sf.net/p/openocd/code openocd "

./ssh " cd openocd && ./bootstrap "
./ssh " cd openocd && ./configure "
./ssh " cd openocd && make "
./ssh " cd openocd && sudo make install "

